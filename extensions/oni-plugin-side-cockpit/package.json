{
    "name": "oni-plugin-side-cockpit",
    "version": "0.0.1",
    "main": "lib/index.js",
    "engines": {
        "oni": "^0.0.1"
    },
    "scripts": {
        "build": "rimraf lib && tsc",
        "test": "tsc -p tsconfig.test.json && mocha --recursive ./lib_test/test"
    }
}
